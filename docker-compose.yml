version: '3.8'

services:
  dashboard:
    build: .
    container_name: h3ow3d-dashboard
    ports:
      - "8080:80"
    restart: unless-stopped
    labels:
      - "com.h3ow3d.description=GitHub Actions Dashboard"
